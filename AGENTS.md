# AGENTS.md — learn-ai-agent 仓库工作约定

## 每日任务收尾流程（每天必须执行）
每天任务验收通过后，自动执行以下 4 步：

1. **整理每日笔记**：创建 `01-每日笔记/YYYY-MM-DD-主题.md`
   - 固定结构：今日目标 / 今天做了什么 / 知识点清单 / 问题串（**按用户当天提问顺序**整理，Q&A 形式）/ 踩坑记录 / 一行收获 / 明天预告
2. **打卡更新**：README.md 学习进度表 → 当天行标 `✅ 已完成`
3. **验收记录**：`06-验收记录.md` → 加一行当天结果
4. **提交推送**：`git add -A` + `git commit -m "M/D 打卡：主题"` + `git push`

## 验收约定
- 用户说"写好了/改好了"或给出交付物 → 打开文件 → 编译运行 → 按验收标准给 ✅ 通过 / 🔧 需修正 / 💡 建议。
- 修正后必须重新编译运行确认，通过才算完成当天任务。

## 网络注意事项
- GitHub 连不上（报错 `Failed to connect to github.com` / `Could not connect to server`）时：
  1. 提醒用户：**打开 Watt Toolkit（Steam++）→ 开启"GitHub 加速"** → 再重试 `git push`
  2. 本地 commit 不丢数据，网络恢复后 push 即可